import { useMemo, useState } from "react";
import { ArrowLeft, BedDouble, CalendarDays, Check, ChevronRight, Clock3, CreditCard, MapPin, QrCode, Search, ShieldCheck, Star, Users, Wifi, X } from "lucide-react";
import { useLocation } from "wouter";
import GlobalCityPicker from "@/components/GlobalCityPicker";

const BG = "#040b14", PANEL = "#0b121d", CARD = "#101925", LINE = "rgba(255,255,255,.09)", TEXT = "#f2f4f7", DIM = "#8e99a8", GOLD = "#c9a84c", GREEN = "#22c55e", PURPLE = "#a78bfa", CYAN = "#22d3ee";
type Filter = "All Stays" | "Luxury Resorts" | "Boutique Hotels" | "Mountain Lodges" | "5-Star Rated";
type Room = { name: string; detail: string; rate: number; tag: string };
type Hotel = { id: string; name: string; location: string; city: string; rating: number; reviews: string; rate: number; category: Filter; accent: string; amenities: string[]; rooms: Room[] };
const filters: Filter[] = ["All Stays", "Luxury Resorts", "Boutique Hotels", "Mountain Lodges", "5-Star Rated"];
const hotels: Hotel[] = [
  { id: "serena-hunza", name: "Serena Hotel & Resort", location: "Hunza Valley", city: "Hunza", rating: 4.8, reviews: "120+ reviews", rate: 25000, category: "Mountain Lodges", accent: "#0e7490", amenities: ["Free WiFi", "Breakfast Included", "Valet Parking", "Pool"], rooms: [{ name: "Deluxe Mountain Room", detail: "King bed · Valley view · 2 guests", rate: 25000, tag: "Best value" }, { name: "Executive Suite", detail: "King bed · Lounge · 3 guests", rate: 42000, tag: "Popular" }, { name: "Family Villa", detail: "2 bedrooms · Private terrace · 6 guests", rate: 68000, tag: "Family pick" }] },
  { id: "islamabad-regal", name: "The Regal Islamabad", location: "Diplomatic Enclave", city: "Islamabad", rating: 4.9, reviews: "340+ reviews", rate: 32000, category: "5-Star Rated", accent: "#6d28d9", amenities: ["Free WiFi", "Breakfast Included", "Valet Parking", "Spa"], rooms: [{ name: "Deluxe City Room", detail: "King bed · City view · 2 guests", rate: 32000, tag: "Best value" }, { name: "Executive Suite", detail: "King bed · Club access · 3 guests", rate: 54000, tag: "Popular" }, { name: "Presidential Suite", detail: "2 bedrooms · Butler service · 4 guests", rate: 105000, tag: "Premium" }] },
  { id: "murree-pines", name: "Pines & Peaks Lodge", location: "Kashmir Point, Murree", city: "Murree", rating: 4.7, reviews: "88+ reviews", rate: 18000, category: "Mountain Lodges", accent: "#15803d", amenities: ["Free WiFi", "Breakfast Included", "Fireplace", "Parking"], rooms: [{ name: "Pine View Room", detail: "Queen bed · Forest view · 2 guests", rate: 18000, tag: "Best value" }, { name: "Family Chalet", detail: "2 bedrooms · Fireplace · 5 guests", rate: 39000, tag: "Popular" }] },
  { id: "skardu-azure", name: "Azure Skardu Retreat", location: "Upper Kachura Road", city: "Skardu", rating: 4.9, reviews: "210+ reviews", rate: 29000, category: "Luxury Resorts", accent: "#1d4ed8", amenities: ["Free WiFi", "Breakfast Included", "Airport Transfer", "Lake View"], rooms: [{ name: "Lake View Room", detail: "King bed · Lake view · 2 guests", rate: 29000, tag: "Best value" }, { name: "Garden Villa", detail: "1 bedroom · Garden · 3 guests", rate: 49000, tag: "Popular" }] },
];
const money = (n: number) => `Rs ${n.toLocaleString("en-PK")}`;

function BookingModal({ hotel, close }: { hotel: Hotel; close: () => void }) {
  const [room, setRoom] = useState(hotel.rooms[0]);
  const [payment, setPayment] = useState("Wallet Balance");
  const [booked, setBooked] = useState(false);
  const [checkIn, setCheckIn] = useState("");
  const [checkOut, setCheckOut] = useState("");
  const nights = checkIn && checkOut ? Math.max(1, Math.ceil((new Date(checkOut).getTime() - new Date(checkIn).getTime()) / 86400000)) : 1;
  const taxes = Math.round(room.rate * nights * .12), total = room.rate * nights + taxes;
  return <div onClick={close} style={{ position: "fixed", inset: 0, zIndex: 100, display: "flex", alignItems: "flex-end", justifyContent: "center", background: "rgba(0,0,0,.8)" }}><div onClick={e => e.stopPropagation()} style={{ width: "100%", maxWidth: 560, maxHeight: "93dvh", overflowY: "auto", padding: 18, borderRadius: "22px 22px 0 0", background: PANEL, border: `1px solid ${LINE}`, color: TEXT }}>
    <div style={{ display: "flex", justifyContent: "space-between" }}><div><div style={{ color: PURPLE, fontSize: 10, fontWeight: 900, letterSpacing: ".13em" }}>ROOMS & RESERVATION</div><h2 style={{ margin: "7px 0 3px", fontSize: 20 }}>{hotel.name}</h2><div style={{ color: DIM, fontSize: 11 }}>{hotel.location} · <Star size={11} fill={GOLD} color={GOLD} style={{ verticalAlign: "-1px" }} /> {hotel.rating}</div></div><button onClick={close} style={{ border: 0, background: "transparent", color: DIM }}><X size={20} /></button></div>
    {!booked ? <><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8, marginTop: 14 }}><label style={{ color: DIM, fontSize: 10 }}>CHECK-IN<input type="date" value={checkIn} onChange={e => setCheckIn(e.target.value)} style={{ display: "block", width: "100%", boxSizing: "border-box", marginTop: 5, padding: 10, borderRadius: 9, border: `1px solid ${LINE}`, background: CARD, color: TEXT }} /></label><label style={{ color: DIM, fontSize: 10 }}>CHECK-OUT<input type="date" value={checkOut} onChange={e => setCheckOut(e.target.value)} style={{ display: "block", width: "100%", boxSizing: "border-box", marginTop: 5, padding: 10, borderRadius: 9, border: `1px solid ${LINE}`, background: CARD, color: TEXT }} /></label></div><div style={{ marginTop: 15, color: DIM, fontSize: 10, fontWeight: 900, letterSpacing: ".12em" }}>AVAILABLE ROOM TYPES</div>{hotel.rooms.map(item => <button key={item.name} onClick={() => setRoom(item)} style={{ width: "100%", display: "flex", justifyContent: "space-between", alignItems: "center", gap: 8, marginTop: 8, padding: 12, textAlign: "left", borderRadius: 11, border: `1px solid ${room.name === item.name ? GOLD : LINE}`, background: room.name === item.name ? `${GOLD}12` : CARD, color: TEXT }}><span><strong style={{ display: "block", fontSize: 13 }}>{item.name}</strong><small style={{ display: "block", color: DIM, marginTop: 4 }}>{item.detail}</small><small style={{ display: "inline-block", color: GREEN, marginTop: 6, fontWeight: 800 }}>{item.tag}</small></span><strong style={{ color: GOLD, whiteSpace: "nowrap" }}>{money(item.rate)}<small style={{ display: "block", color: DIM, fontSize: 9, textAlign: "right" }}>/ night</small></strong></button>)}<div style={{ marginTop: 14, padding: 12, borderRadius: 11, background: CARD, border: `1px solid ${LINE}` }}><div style={{ color: DIM, fontSize: 10, fontWeight: 900, letterSpacing: ".12em" }}>RATE BREAKDOWN · {nights} NIGHT{nights > 1 ? "S" : ""}</div>{[[`Room × ${nights}`, money(room.rate * nights)], ["Taxes & service fee", money(taxes)], ["Total payable", money(total)]].map(([l, v], i) => <div key={l} style={{ display: "flex", justifyContent: "space-between", marginTop: 8, fontSize: i === 2 ? 14 : 11, color: i === 2 ? TEXT : DIM, fontWeight: i === 2 ? 900 : 500 }}><span>{l}</span><strong>{v}</strong></div>)}</div><div style={{ display: "flex", gap: 7, marginTop: 12, overflowX: "auto" }}>{["Wallet Balance", "Debit / Credit Card", "Bank Transfer"].map(item => <button key={item} onClick={() => setPayment(item)} style={{ flex: 1, minWidth: 105, padding: 9, borderRadius: 9, border: `1px solid ${payment === item ? GOLD : LINE}`, background: payment === item ? `${GOLD}14` : CARD, color: payment === item ? GOLD : DIM, fontSize: 9, fontWeight: 800 }}><CreditCard size={12} style={{ verticalAlign: "-2px", marginRight: 3 }} />{item}</button>)}</div><div style={{ display: "flex", gap: 6, alignItems: "center", marginTop: 11, color: DIM, fontSize: 10 }}><Clock3 size={13} />Free cancellation until 48 hours before check-in · Check-in from 2:00 PM</div><button onClick={() => setBooked(true)} style={{ width: "100%", marginTop: 14, padding: 13, border: 0, borderRadius: 11, background: GOLD, color: BG, fontWeight: 900 }}>Confirm Booking & Pay · {money(total)}</button></> : <div style={{ marginTop: 18, padding: 18, borderRadius: 15, background: `${GREEN}0d`, border: `1px solid ${GREEN}40`, textAlign: "center" }}><Check size={32} color={GREEN} /><h3 style={{ margin: "8px 0 3px", fontSize: 18 }}>Booking confirmed</h3><p style={{ color: DIM, fontSize: 11, lineHeight: 1.5 }}>Your digital booking voucher is ready. Payment method: {payment}.</p><div style={{ display: "grid", placeItems: "center", width: 118, height: 118, margin: "15px auto", borderRadius: 12, background: "#fff" }}><QrCode size={82} color="#111827" /></div><div style={{ padding: 11, borderRadius: 9, background: CARD, textAlign: "left", fontSize: 11, lineHeight: 1.8 }}><strong>Voucher ID: OKZ-{hotel.id.toUpperCase().slice(0, 6)}-{Date.now().toString().slice(-5)}</strong><br /><span style={{ color: DIM }}>{room.name} · {nights} night{nights > 1 ? "s" : ""}<br />{checkIn || "Flexible check-in"} → {checkOut || "Flexible check-out"}</span></div><button onClick={close} style={{ marginTop: 14, padding: "10px 22px", border: 0, borderRadius: 10, background: GOLD, color: BG, fontWeight: 900 }}>Done</button></div>}
  </div></div>;
}

export default function HotelStays() { const [, navigate] = useLocation(); const [query, setQuery] = useState(""); const [filter, setFilter] = useState<Filter>("All Stays"); const [checkIn, setCheckIn] = useState(""); const [checkOut, setCheckOut] = useState(""); const [guests, setGuests] = useState("1 Room, 2 Guests"); const [selected, setSelected] = useState<Hotel | null>(null); const visible = useMemo(() => hotels.filter(h => { const text = `${h.name} ${h.location} ${h.city} ${h.category}`.toLowerCase(); return text.includes(query.toLowerCase()) && (filter === "All Stays" || h.category === filter || (filter === "5-Star Rated" && h.rating >= 4.8)); }), [filter, query]); return <main style={{ minHeight: "100dvh", padding: "10px 14px calc(92px + env(safe-area-inset-bottom))", background: BG, color: TEXT, fontFamily: "Inter, Plus Jakarta Sans, sans-serif" }}><div style={{ maxWidth: 760, margin: "0 auto" }}><header style={{ display: "flex", alignItems: "center", gap: 11, height: 54, borderBottom: `1px solid ${LINE}` }}><button onClick={() => navigate("/market/services")} style={{ border: 0, background: "transparent", color: DIM, padding: 6 }}><ArrowLeft size={21} /></button><div><span style={{ display: "block", color: PURPLE, fontSize: 10, fontWeight: 900, letterSpacing: ".13em" }}>STAY DESK</span><h1 style={{ margin: "4px 0 0", fontSize: 21 }}>Hotels & Resorts</h1></div></header><section style={{ marginTop: 15, padding: 13, borderRadius: 15, background: PANEL, border: `1px solid ${LINE}` }}><div style={{ position: "relative" }}><Search size={16} color={DIM} style={{ position: "absolute", left: 11, top: 12 }} /><GlobalCityPicker value={query} onChange={(value) => setQuery(value)} placeholder="Search or select destination city" style={{ width: "100%" }} /></div><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8, marginTop: 9 }}><label style={{ color: DIM, fontSize: 9 }}>CHECK-IN<input type="date" value={checkIn} onChange={e => setCheckIn(e.target.value)} style={{ display: "block", width: "100%", boxSizing: "border-box", marginTop: 4, padding: 9, borderRadius: 8, border: `1px solid ${LINE}`, background: CARD, color: TEXT }} /></label><label style={{ color: DIM, fontSize: 9 }}>CHECK-OUT<input type="date" value={checkOut} onChange={e => setCheckOut(e.target.value)} style={{ display: "block", width: "100%", boxSizing: "border-box", marginTop: 4, padding: 9, borderRadius: 8, border: `1px solid ${LINE}`, background: CARD, color: TEXT }} /></label></div><button onClick={() => setGuests(guests === "1 Room, 2 Guests" ? "2 Rooms, 4 Guests" : "1 Room, 2 Guests")} style={{ width: "100%", display: "flex", alignItems: "center", gap: 7, marginTop: 9, padding: 10, borderRadius: 9, border: `1px solid ${LINE}`, background: CARD, color: DIM, textAlign: "left", fontSize: 11 }}><Users size={14} />{guests}<ChevronRight size={14} style={{ marginLeft: "auto" }} /></button></section><div style={{ display: "flex", gap: 7, overflowX: "auto", padding: "14px 0 8px", scrollbarWidth: "none" }}>{filters.map(item => <button key={item} onClick={() => setFilter(item)} style={{ flexShrink: 0, whiteSpace: "nowrap", padding: "9px 11px", borderRadius: 10, border: `1px solid ${filter === item ? `${PURPLE}99` : LINE}`, background: filter === item ? `${PURPLE}18` : PANEL, color: filter === item ? PURPLE : DIM, fontSize: 10, fontWeight: 800 }}>{item}</button>)}</div><section style={{ display: "flex", justifyContent: "space-between", alignItems: "end", margin: "14px 0 11px" }}><div><div style={{ color: PURPLE, fontSize: 10, fontWeight: 900, letterSpacing: ".12em" }}>VERIFIED STAYS</div><h2 style={{ margin: "5px 0 0", fontSize: 18 }}>Reserve with confidence</h2></div><span style={{ color: DIM, fontSize: 10 }}>{visible.length} stays</span></section><div style={{ display: "grid", gap: 13 }}>{visible.map(hotel => <article key={hotel.id} style={{ overflow: "hidden", borderRadius: 16, background: CARD, border: `1px solid ${LINE}` }}><div style={{ height: 135, display: "flex", alignItems: "flex-end", justifyContent: "space-between", padding: 12, background: `linear-gradient(135deg, ${hotel.accent}99, #07101c 68%), repeating-linear-gradient(45deg, transparent 0 26px, rgba(255,255,255,.1) 27px 28px)` }}><span style={{ padding: "6px 8px", borderRadius: 8, background: "rgba(0,0,0,.65)", color: GOLD, fontSize: 10, fontWeight: 900 }}><Star size={12} fill={GOLD} style={{ verticalAlign: "-2px", marginRight: 3 }} />{hotel.rating} · {hotel.reviews}</span><span style={{ padding: "6px 8px", borderRadius: 8, background: "rgba(0,0,0,.65)", color: TEXT, fontSize: 9, fontWeight: 800 }}>{hotel.category}</span></div><div style={{ padding: 14 }}><div style={{ display: "flex", justifyContent: "space-between", gap: 10 }}><div><h3 style={{ margin: 0, fontSize: 16 }}>{hotel.name}</h3><div style={{ display: "flex", alignItems: "center", gap: 4, color: DIM, fontSize: 10, marginTop: 5 }}><MapPin size={13} />{hotel.location} · {hotel.city}</div></div><div style={{ textAlign: "right", whiteSpace: "nowrap" }}><strong style={{ color: GOLD, fontSize: 15 }}>{money(hotel.rate)}</strong><small style={{ display: "block", color: DIM, fontSize: 9 }}>per night · taxes included</small></div></div><div style={{ display: "flex", gap: 6, flexWrap: "wrap", marginTop: 11 }}>{hotel.amenities.map(a => <span key={a} style={{ padding: "5px 7px", borderRadius: 7, background: `${CYAN}10`, color: CYAN, fontSize: 9, fontWeight: 800 }}>{a}</span>)}</div><div style={{ display: "flex", gap: 8, marginTop: 13 }}><button onClick={() => setSelected(hotel)} style={{ flex: 1, padding: 10, border: `1px solid ${PURPLE}66`, borderRadius: 10, background: `${PURPLE}12`, color: PURPLE, fontSize: 10, fontWeight: 900 }}><BedDouble size={13} style={{ verticalAlign: "-2px", marginRight: 4 }} />View Rooms</button><button onClick={() => setSelected(hotel)} style={{ flex: 1, padding: 10, border: 0, borderRadius: 10, background: GOLD, color: BG, fontSize: 10, fontWeight: 900 }}>Instant Book</button></div></div></article>)}</div>{visible.length === 0 && <div style={{ padding: 28, marginTop: 12, borderRadius: 14, background: CARD, border: `1px solid ${LINE}`, textAlign: "center", color: DIM, fontSize: 12 }}>No stays match this destination or category.</div>}<div style={{ display: "flex", gap: 8, alignItems: "center", marginTop: 16, padding: 12, borderRadius: 12, background: PANEL, border: `1px solid ${LINE}`, color: DIM, fontSize: 10, lineHeight: 1.5 }}><ShieldCheck size={16} color={GREEN} />Verified rates, cancellation terms, guest capacity, and reservation confirmation are presented before payment.</div></div>{selected && <BookingModal hotel={selected} close={() => setSelected(null)} />}</main>; }
