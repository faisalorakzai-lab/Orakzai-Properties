import { useMemo, useState } from "react";
import { ArrowLeft, BadgeCheck, Calculator, Check, ChevronRight, ClipboardList, Download, Droplets, Gem, MapPin, MessageCircle, Search, ShieldCheck, Sparkles, Truck, X } from "lucide-react";
import { useLocation } from "wouter";

const BG = "#040b14";
const PANEL = "#0b121d";
const CARD = "#101925";
const LINE = "rgba(255,255,255,.09)";
const TEXT = "#f2f4f7";
const DIM = "#8e99a8";
const GOLD = "#c9a84c";
const CYAN = "#06b6d4";
const GREEN = "#22c55e";
const PINK = "#ec4899";

type Category = "All Finishes" | "Porcelain & Ceramic Tiles" | "Sanitary Ware & Faucets" | "Marble & Granite Slabs" | "Vanity & Bathroom Units";
type FinishProduct = { id: string; title: string; brand: string; origin: string; category: Exclude<Category, "All Finishes">; price: number; unit: string; finish: string; color: string; grade: string; specs: string; coverage: number; badge: string; image: string; accent: string; supplier: string; rating: number; };

const CATEGORIES: Category[] = ["All Finishes", "Porcelain & Ceramic Tiles", "Sanitary Ware & Faucets", "Marble & Granite Slabs", "Vanity & Bathroom Units"];
const PRODUCTS: FinishProduct[] = [
  { id: "spanish-porcelain", title: "Spanish Glazed Porcelain Tile 2×4 ft", brand: "Azulev España", origin: "Imported · Spain", category: "Porcelain & Ceramic Tiles", price: 350, unit: "Sq. Ft.", finish: "Glossy", color: "Ivory", grade: "Premium Grade", specs: "Water resistant · Rectified edges · Low maintenance", coverage: 16, badge: "Imported Quality", image: "https://images.unsplash.com/photo-1618221195710-dd6b41faaea6?auto=format&fit=crop&w=1400&q=85", accent: CYAN, supplier: "Casa Ceramics Showroom", rating: 4.9 },
  { id: "master-matte", title: "Urban Stone Matte Porcelain Tile 2×2 ft", brand: "Master Tiles", origin: "Local Premium · Pakistan", category: "Porcelain & Ceramic Tiles", price: 295, unit: "Sq. Ft.", finish: "Matte", color: "Warm Grey", grade: "A+ Grade", specs: "Anti-slip R10 · Heavy-duty · Frost resistant", coverage: 16, badge: "Local Premium", image: "https://images.unsplash.com/photo-1552321554-5fefe8c9ef14?auto=format&fit=crop&w=1400&q=85", accent: GOLD, supplier: "Master Tiles Authorized Partner", rating: 4.8 },
  { id: "sonex-suite", title: "Sonex Basin & Mixer Suite", brand: "Sonex", origin: "Local Premium · Pakistan", category: "Sanitary Ware & Faucets", price: 28000, unit: "Unit", finish: "Glossy Ceramic", color: "Alpine White", grade: "Premium Grade", specs: "Water-saving mixer · Soft-close waste · 5-year warranty", coverage: 1, badge: "Local Premium", image: "https://images.unsplash.com/photo-1584622650111-993a426fbf0a?auto=format&fit=crop&w=1400&q=85", accent: PINK, supplier: "Sonex Official Showroom", rating: 4.8 },
  { id: "botticino-marble", title: "Botticino Marble Slab – Honed", brand: "Royal Stone Gallery", origin: "Imported · Italy", category: "Marble & Granite Slabs", price: 1250, unit: "Sq. Ft.", finish: "Honed", color: "Cream", grade: "Select Slab", specs: "Natural veining · Sealed surface · Bookmatch available", coverage: 18, badge: "Imported Quality", image: "https://images.unsplash.com/photo-1600566753086-00f18fb6b3ea?auto=format&fit=crop&w=1400&q=85", accent: "#a78bfa", supplier: "Royal Stone Gallery", rating: 4.9 },
  { id: "kohler-faucet", title: "Kohler Wall-Mount Faucet & Shower Set", brand: "Kohler", origin: "Imported · USA", category: "Sanitary Ware & Faucets", price: 68500, unit: "Unit", finish: "Brushed Nickel", color: "Chrome", grade: "Luxury Grade", specs: "Thermostatic control · WaterSense · Lifetime finish", coverage: 1, badge: "Imported Quality", image: "https://images.unsplash.com/photo-1620626011761-996317b8d101?auto=format&fit=crop&w=1400&q=85", accent: "#60a5fa", supplier: "Kohler Design Partner", rating: 4.9 },
  { id: "walnut-vanity", title: "Walnut Floating Vanity & Countertop", brand: "Form Living", origin: "Local Premium · Pakistan", category: "Vanity & Bathroom Units", price: 94000, unit: "Unit", finish: "Natural Wood", color: "Walnut", grade: "Bespoke Grade", specs: "Marine plywood · Quartz top · Soft-close drawers", coverage: 1, badge: "Local Premium", image: "https://images.unsplash.com/photo-1558997519-83ea9252edf8?auto=format&fit=crop&w=1400&q=85", accent: GREEN, supplier: "Form Living Studio", rating: 4.7 },
];

function money(value: number) { return `Rs ${Math.round(value).toLocaleString("en-PK")}`; }
function makeEstimatePdf(product: FinishProduct, length: number, width: number, area: number, boxes: number, total: number) {
  const lines = ["OKZBYTE · FINISHING MATERIALS ESTIMATE", `Estimate date: ${new Date().toLocaleDateString("en-PK")}`, `Product: ${product.title}`, `Brand / Origin: ${product.brand} · ${product.origin}`, `Room dimensions: ${length} ft × ${width} ft`, `Net area: ${area.toFixed(1)} sq. ft.`, "Waste factor: 10% included", `Estimated boxes / units: ${boxes}`, `Estimated material value: ${money(total)}`, "", "Final quantity, shade, availability, delivery, and installation are confirmed by the showroom."];
  const esc = (value: string) => value.replace(/\\/g, "\\\\").replace(/\(/g, "\\(").replace(/\)/g, "\\)");
  const stream = ["BT", "/F1 11 Tf", "50 760 Td", ...lines.map((line, index) => `${index ? "0 -18 Td " : ""}(${esc(line)}) Tj`), "ET"].join(" ");
  return new Blob([`%PDF-1.4\n1 0 obj<< /Type /Catalog /Pages 2 0 R>>endobj\n2 0 obj<< /Type /Pages /Kids [3 0 R] /Count 1>>endobj\n3 0 obj<< /Type /Page /Parent 2 0 R /MediaBox [0 0 612 792] /Resources<< /Font<< /F1 4 0 R>>>> /Contents 5 0 R>>endobj\n4 0 obj<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica>>endobj\n5 0 obj<< /Length ${stream.length}>>stream\n${stream}\nendstream endobj\ntrailer<< /Root 1 0 R>>\n%%EOF`], { type: "application/pdf" });
}

function FinishModal({ product, close }: { product: FinishProduct; close: () => void }) {
  const [, navigate] = useLocation();
  const [mode, setMode] = useState<"calculator" | "quote">("calculator");
  const [length, setLength] = useState(12);
  const [width, setWidth] = useState(12);
  const [notes, setNotes] = useState("");
  const [done, setDone] = useState(false);
  const area = Math.max(1, length * width);
  const wasteArea = area * 1.1;
  const boxes = Math.max(1, Math.ceil(wasteArea / product.coverage));
  const estimatedArea = product.unit === "Sq. Ft." ? boxes * product.coverage : boxes;
  const total = estimatedArea * product.price;
  const requestQuote = () => {
    const blob = makeEstimatePdf(product, length, width, area, boxes, total);
    const url = URL.createObjectURL(blob);
    const anchor = document.createElement("a");
    anchor.href = url;
    anchor.download = `okzbyte-${product.id}-showroom-estimate.pdf`;
    anchor.click();
    URL.revokeObjectURL(url);
    localStorage.setItem("okzbyte_finish_quote", JSON.stringify({ product: product.id, length, width, boxes, total, notes, createdAt: new Date().toISOString() }));
    setDone(true);
  };
  return <div onClick={close} style={{ position: "fixed", inset: 0, zIndex: 90, background: "rgba(0,0,0,.78)", display: "flex", alignItems: "flex-end", justifyContent: "center" }}><div onClick={(event) => event.stopPropagation()} style={{ width: "100%", maxWidth: 560, maxHeight: "94dvh", overflowY: "auto", padding: 18, borderRadius: "22px 22px 0 0", background: PANEL, border: `1px solid ${LINE}`, color: TEXT }}><div style={{ display: "flex", justifyContent: "space-between", gap: 12 }}><div><div style={{ color: CYAN, fontSize: 10, fontWeight: 900, letterSpacing: ".14em" }}>SHOWROOM ESTIMATE DESK</div><h2 style={{ margin: "7px 0 3px", fontSize: 18, lineHeight: 1.3 }}>{product.title}</h2><div style={{ color: DIM, fontSize: 10 }}>{product.brand} · {product.origin} · {money(product.price)} / {product.unit}</div></div><button aria-label="Close" onClick={close} style={{ border: 0, background: "transparent", color: DIM }}><X size={20} /></button></div>{done ? <div style={{ marginTop: 17, padding: 17, borderRadius: 14, background: `${GREEN}0d`, border: `1px solid ${GREEN}40`, textAlign: "center" }}><Check size={30} color={GREEN} /><h3 style={{ margin: "8px 0 4px", fontSize: 17 }}>Store quotation prepared</h3><p style={{ margin: 0, color: DIM, fontSize: 11, lineHeight: 1.5 }}>Your estimate was saved to the dashboard and downloaded. The showroom team can confirm shade, stock, delivery, and installation in Inbox.</p><div style={{ marginTop: 14, display: "flex", gap: 8 }}><button onClick={close} style={{ flex: 1, padding: 11, borderRadius: 10, border: `1px solid ${LINE}`, background: CARD, color: DIM, fontWeight: 800 }}>Close</button><button onClick={() => navigate(`/inbox/1?context=finish-${product.id}`)} style={{ flex: 1, padding: 11, border: 0, borderRadius: 10, background: GOLD, color: BG, fontWeight: 900 }}><MessageCircle size={13} style={{ verticalAlign: "-2px", marginRight: 4 }} />Open Supplier Inbox</button></div></div> : <><div style={{ display: "flex", gap: 7, marginTop: 16 }}><button onClick={() => setMode("calculator")} style={{ flex: 1, padding: 10, borderRadius: 10, border: `1px solid ${mode === "calculator" ? `${CYAN}99` : LINE}`, background: mode === "calculator" ? `${CYAN}18` : CARD, color: mode === "calculator" ? CYAN : DIM, fontWeight: 800, fontSize: 10 }}><Calculator size={13} style={{ verticalAlign: "-2px", marginRight: 4 }} />Area Calculator</button><button onClick={() => setMode("quote")} style={{ flex: 1, padding: 10, borderRadius: 10, border: `1px solid ${mode === "quote" ? `${GOLD}99` : LINE}`, background: mode === "quote" ? `${GOLD}18` : CARD, color: mode === "quote" ? GOLD : DIM, fontWeight: 800, fontSize: 10 }}><ClipboardList size={13} style={{ verticalAlign: "-2px", marginRight: 4 }} />Order / Quote</button></div><section style={{ marginTop: 14, padding: 14, borderRadius: 14, background: CARD, border: `1px solid ${LINE}` }}><div style={{ color: DIM, fontSize: 10, fontWeight: 900 }}>ROOM DIMENSIONS · WASTE FACTOR 10%</div><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 9, marginTop: 11 }}><label style={{ color: DIM, fontSize: 10 }}>LENGTH (FT)<input type="number" min={1} value={length} onChange={(event) => setLength(Math.max(1, Number(event.target.value) || 1))} style={{ display: "block", width: "100%", boxSizing: "border-box", marginTop: 5, padding: 11, borderRadius: 9, border: `1px solid ${LINE}`, background: PANEL, color: TEXT }} /></label><label style={{ color: DIM, fontSize: 10 }}>WIDTH (FT)<input type="number" min={1} value={width} onChange={(event) => setWidth(Math.max(1, Number(event.target.value) || 1))} style={{ display: "block", width: "100%", boxSizing: "border-box", marginTop: 5, padding: 11, borderRadius: 9, border: `1px solid ${LINE}`, background: PANEL, color: TEXT }} /></label></div><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8, marginTop: 12 }}><div style={{ padding: 11, borderRadius: 10, background: PANEL }}><div style={{ color: DIM, fontSize: 9 }}>NET AREA</div><strong style={{ display: "block", marginTop: 4, fontSize: 15 }}>{area.toFixed(1)} sq. ft.</strong></div><div style={{ padding: 11, borderRadius: 10, background: `${CYAN}10` }}><div style={{ color: DIM, fontSize: 9 }}>EST. BOXES / UNITS</div><strong style={{ display: "block", marginTop: 4, color: CYAN, fontSize: 15 }}>{boxes} {product.unit === "Sq. Ft." ? "boxes" : "units"}</strong></div></div></section><div style={{ marginTop: 11, padding: 12, borderRadius: 11, background: PANEL, border: `1px solid ${LINE}` }}><div style={{ display: "flex", justifyContent: "space-between", color: DIM, fontSize: 10 }}><span>10% WASTE INCLUDED</span><span>{product.coverage} sq. ft. / box reference</span></div><div style={{ display: "flex", justifyContent: "space-between", alignItems: "end", marginTop: 9 }}><span style={{ color: DIM, fontSize: 11 }}>Estimated material value</span><strong style={{ color: CYAN, fontSize: 21 }}>{money(total)}</strong></div></div>{mode === "calculator" ? <button onClick={() => setMode("quote")} style={{ width: "100%", marginTop: 14, padding: 13, border: 0, borderRadius: 12, background: CYAN, color: BG, fontWeight: 900 }}>Continue to quotation</button> : <><label style={{ display: "block", marginTop: 14, color: DIM, fontSize: 10, fontWeight: 800 }}>PROJECT NOTES<textarea value={notes} onChange={(event) => setNotes(event.target.value)} placeholder="Room name, preferred shade, delivery timeline, or installation requirements..." rows={3} style={{ display: "block", width: "100%", boxSizing: "border-box", marginTop: 7, padding: 12, borderRadius: 10, border: `1px solid ${LINE}`, background: CARD, color: TEXT, resize: "vertical" }} /></label><button onClick={requestQuote} style={{ width: "100%", marginTop: 14, padding: 13, border: 0, borderRadius: 12, background: GOLD, color: BG, fontWeight: 900 }}><Download size={14} style={{ verticalAlign: "-2px", marginRight: 4 }} />Request Store Quotation</button></>}</>}</div></div>;
}

export default function TilesSanitary() {
  const [, navigate] = useLocation();
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState<Category>("All Finishes");
  const [finish, setFinish] = useState("All Finishes");
  const [color, setColor] = useState("All Colors");
  const [grade, setGrade] = useState("All Grades");
  const [selected, setSelected] = useState<FinishProduct | null>(null);
  const products = useMemo(() => PRODUCTS.filter((product) => (category === "All Finishes" || product.category === category) && (finish === "All Finishes" || product.finish === finish) && (color === "All Colors" || product.color === color) && (grade === "All Grades" || product.grade === grade) && `${product.title} ${product.brand} ${product.origin} ${product.finish} ${product.color} ${product.grade}`.toLowerCase().includes(query.toLowerCase())), [query, category, finish, color, grade]);
  return <main style={{ minHeight: "100dvh", background: BG, color: TEXT, fontFamily: "Inter, Plus Jakarta Sans, sans-serif", paddingBottom: 92 }}><div style={{ maxWidth: 640, margin: "0 auto", padding: "14px 15px 0" }}><header style={{ display: "flex", alignItems: "center", gap: 12, paddingBottom: 15, borderBottom: `1px solid ${LINE}` }}><button aria-label="Back to Services" onClick={() => navigate("/market/services")} style={{ border: 0, background: "transparent", color: DIM, padding: 0 }}><ArrowLeft size={22} /></button><div><div style={{ color: CYAN, fontSize: 10, fontWeight: 900, letterSpacing: ".14em" }}>INTERIORS · FINISHING DESK</div><h1 style={{ margin: "4px 0 0", fontSize: 21 }}>Tiles, Sanitary & Marble</h1><div style={{ color: DIM, fontSize: 10, marginTop: 3 }}>Architectural finishes from verified showrooms</div></div></header><section style={{ marginTop: 15, padding: 13, borderRadius: 15, background: PANEL, border: `1px solid ${LINE}` }}><div style={{ position: "relative" }}><Search size={17} color={DIM} style={{ position: "absolute", left: 13, top: 13 }} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search tiles, marble, sanitary, brand..." style={{ width: "100%", boxSizing: "border-box", padding: "12px 13px 12px 38px", borderRadius: 11, border: `1px solid ${LINE}`, background: CARD, color: TEXT }} /></div><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 7, marginTop: 9 }}><select value={finish} onChange={(event) => setFinish(event.target.value)} style={{ width: "100%", padding: 10, borderRadius: 9, border: `1px solid ${LINE}`, background: CARD, color: TEXT, fontSize: 10 }}><option>All Finishes</option><option>Glossy</option><option>Matte</option><option>Honed</option><option>Brushed Nickel</option><option>Natural Wood</option><option>Glossy Ceramic</option></select><select value={color} onChange={(event) => setColor(event.target.value)} style={{ width: "100%", padding: 10, borderRadius: 9, border: `1px solid ${LINE}`, background: CARD, color: TEXT, fontSize: 10 }}><option>All Colors</option><option>Ivory</option><option>Warm Grey</option><option>Alpine White</option><option>Cream</option><option>Chrome</option><option>Walnut</option></select><select value={grade} onChange={(event) => setGrade(event.target.value)} style={{ width: "100%", padding: 10, borderRadius: 9, border: `1px solid ${LINE}`, background: CARD, color: TEXT, fontSize: 10 }}><option>All Grades</option><option>Premium Grade</option><option>A+ Grade</option><option>Select Slab</option><option>Luxury Grade</option><option>Bespoke Grade</option></select></div></section><div style={{ display: "flex", gap: 7, overflowX: "auto", padding: "15px 0 7px", scrollbarWidth: "none" }}>{CATEGORIES.map((item) => <button key={item} onClick={() => setCategory(item)} style={{ whiteSpace: "nowrap", padding: "9px 11px", borderRadius: 10, border: `1px solid ${category === item ? `${CYAN}88` : LINE}`, background: category === item ? `${CYAN}18` : PANEL, color: category === item ? CYAN : DIM, fontSize: 10, fontWeight: 800 }}>{item}</button>)}</div><div style={{ display: "flex", alignItems: "end", justifyContent: "space-between", margin: "14px 0 11px" }}><div><div style={{ color: CYAN, fontSize: 10, fontWeight: 900, letterSpacing: ".12em" }}>SHOWROOM COLLECTION</div><h2 style={{ margin: "5px 0 0", fontSize: 17 }}>Architectural finishes</h2></div><span style={{ color: DIM, fontSize: 10 }}>{products.length} products</span></div><div style={{ display: "grid", gap: 13 }}>{products.map((product) => <article key={product.id} style={{ overflow: "hidden", borderRadius: 16, background: CARD, border: `1px solid ${LINE}` }}><div style={{ position: "relative" }}><img src={product.image} alt={product.title} style={{ width: "100%", height: 145, objectFit: "cover", display: "block" }} /><div style={{ position: "absolute", left: 11, bottom: 10, display: "flex", gap: 6 }}><span style={{ padding: "6px 8px", borderRadius: 8, background: "rgba(0,0,0,.75)", color: product.badge === "Imported Quality" ? GOLD : GREEN, fontSize: 9, fontWeight: 900 }}><Sparkles size={11} style={{ verticalAlign: "-2px", marginRight: 3 }} />{product.badge}</span></div></div><div style={{ padding: 14 }}><div style={{ display: "flex", justifyContent: "space-between", gap: 8 }}><div><h3 style={{ margin: 0, fontSize: 15, lineHeight: 1.3 }}>{product.title}</h3><div style={{ marginTop: 5, color: DIM, fontSize: 10 }}>{product.brand} · {product.origin}</div></div><div style={{ textAlign: "right", whiteSpace: "nowrap" }}><strong style={{ color: CYAN, fontSize: 14 }}>{money(product.price)}</strong><small style={{ display: "block", color: DIM, fontSize: 9 }}>/{product.unit}</small></div></div><div style={{ display: "flex", gap: 6, flexWrap: "wrap", marginTop: 11 }}><span style={{ padding: "6px 8px", borderRadius: 8, background: `${CYAN}14`, color: CYAN, fontSize: 9, fontWeight: 900 }}>Finish: {product.finish}</span><span style={{ padding: "6px 8px", borderRadius: 8, background: PANEL, color: DIM, fontSize: 9, fontWeight: 800 }}>{product.color}</span><span style={{ padding: "6px 8px", borderRadius: 8, background: `${GREEN}12`, color: GREEN, fontSize: 9, fontWeight: 900 }}>{product.grade}</span></div><div style={{ marginTop: 10, padding: 10, borderRadius: 9, background: PANEL, color: DIM, fontSize: 10, lineHeight: 1.45 }}><strong style={{ color: TEXT, fontSize: 10 }}>Specifications</strong><br />{product.specs}</div><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 8, marginTop: 11, paddingTop: 10, borderTop: `1px solid ${LINE}`, color: DIM, fontSize: 10 }}><span><BadgeCheck size={13} color={GREEN} style={{ verticalAlign: "-2px", marginRight: 4 }} />{product.supplier} · <strong style={{ color: GOLD }}>★ {product.rating}</strong></span><span style={{ color: GOLD, fontWeight: 900 }}><Truck size={12} style={{ verticalAlign: "-2px", marginRight: 3 }} />Sample available</span></div><div style={{ display: "flex", gap: 8, marginTop: 12 }}><button onClick={() => setSelected(product)} style={{ flex: 1, padding: 10, borderRadius: 10, border: `1px solid ${CYAN}66`, background: `${CYAN}12`, color: CYAN, fontSize: 10, fontWeight: 900 }}><Calculator size={13} style={{ verticalAlign: "-3px", marginRight: 4 }} />Area Calculator</button><button onClick={() => setSelected(product)} style={{ flex: 1, padding: 10, border: 0, borderRadius: 10, background: GOLD, color: BG, fontSize: 10, fontWeight: 900 }}><ClipboardList size={13} style={{ verticalAlign: "-3px", marginRight: 4 }} />Add to Order / Quote</button></div></div></article>)}</div>{products.length === 0 && <div style={{ padding: 28, marginTop: 12, borderRadius: 14, background: CARD, border: `1px solid ${LINE}`, textAlign: "center", color: DIM, fontSize: 12 }}>No finishing products match the selected filters.</div>}<div style={{ display: "flex", gap: 8, alignItems: "center", marginTop: 16, padding: 12, borderRadius: 12, background: PANEL, border: `1px solid ${LINE}`, color: DIM, fontSize: 10, lineHeight: 1.5 }}><ShieldCheck size={16} color={CYAN} style={{ flexShrink: 0 }} />Showroom rates are indicative. Final shade, stock, delivery, installation, and natural-stone batch matching are confirmed before settlement.</div></div>{selected && <FinishModal product={selected} close={() => setSelected(null)} />}</main>;
}
